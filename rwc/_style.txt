<style>
  /* ============================================================
     PLACEHOLDER CONTENT — easy to swap.
     Brand colours are defined as CSS variables below.
     ============================================================ */
  :root{
    --green:#1c4332;        /* primary dark green */
    --green-dark:#143026;
    --green-light:#2c6347;
    --ink:#1f2622;          /* body text */
    --muted:#5c6b63;        /* secondary text */
    --line:#e4e9e6;         /* hairlines / borders */
    --bg:#ffffff;
    --bg-soft:#f5f8f6;      /* soft section bg */
    --radius:14px;
    --maxw:1200px;
    --font:'Segoe UI',-apple-system,BlinkMacSystemFont,Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  html{scroll-behavior:smooth}
  body{font-family:var(--font);color:var(--ink);background:var(--bg);line-height:1.6;-webkit-font-smoothing:antialiased}
  a{text-decoration:none;color:inherit}
  img{max-width:100%;display:block}
  .wrap{max-width:var(--maxw);margin:0 auto;padding:0 28px}
  .green{color:var(--green)}

  /* ---------- Buttons ---------- */
  .btn{display:inline-flex;align-items:center;gap:10px;font-size:1rem;font-weight:600;
       padding:15px 26px;border-radius:10px;cursor:pointer;border:2px solid transparent;transition:.18s}
  .btn-primary{background:var(--green);color:#fff}
  .btn-primary:hover{background:var(--green-dark)}
  .btn-ghost{background:transparent;color:var(--ink);border-color:var(--ink)}
  .btn-ghost:hover{background:var(--ink);color:#fff}
  .btn-white{background:#fff;color:var(--green)}
  .btn-white:hover{background:#eef3f0}

  /* ---------- Header ---------- */
  header{position:sticky;top:0;z-index:50;background:rgba(255,255,255,.96);backdrop-filter:blur(6px);border-bottom:1px solid var(--line)}
  .nav{display:flex;align-items:center;justify-content:space-between;height:92px;gap:20px}
  .logo{display:flex;align-items:center;gap:10px;font-weight:800;color:var(--green);line-height:.95;font-size:1.45rem;letter-spacing:.5px}
  .logo svg{width:54px;height:54px;flex:none}
  .logo-img{height:58px;width:auto;display:block}
  .logo span small{display:block;font-size:1.45rem}
  .navlinks{display:flex;align-items:center;gap:30px;font-weight:500}
  .navlinks a{position:relative;padding:6px 0;color:#2a312d}
  .navlinks a.active{color:var(--green);font-weight:600}
  .navlinks a.active::after{content:"";position:absolute;left:0;right:0;bottom:-2px;height:2px;background:var(--green)}
  .navlinks a:hover{color:var(--green)}
  .has-caret::after{content:" ▾";font-size:.7em;color:var(--muted)}
  .nav-right{display:flex;align-items:center;gap:22px}
  .phone{display:flex;align-items:center;gap:10px;font-weight:700;font-size:1.15rem;color:var(--ink)}
  .phone .ic{width:38px;height:38px;border-radius:50%;background:var(--bg-soft);display:grid;place-items:center}
  .divider{width:1px;height:34px;background:var(--line)}
  .menu-btn{display:none;background:none;border:0;cursor:pointer;flex-direction:column;gap:5px;padding:8px}
  .menu-btn span{width:26px;height:2px;background:var(--ink);display:block}

  /* ---------- Hero ---------- */
  .hero{position:relative;overflow:hidden}
  .hero .wrap{display:grid;grid-template-columns:1fr 1fr;gap:30px;align-items:center;min-height:600px}
  .hero-copy{padding:60px 0;position:relative;z-index:2}
  .hero .badge{display:inline-flex;align-items:center;gap:9px;font-size:.9rem;font-weight:700;color:var(--green);background:#e7efe9;padding:8px 16px;border-radius:30px;margin-bottom:22px}
  .hero .badge svg{width:18px;height:18px;color:var(--green);flex:none}
  .hero h1{font-size:clamp(2.6rem,5vw,4rem);line-height:1.05;color:var(--green);font-weight:800;letter-spacing:-1px}
  .hero .lead{font-size:1.18rem;color:var(--muted);margin:22px 0 18px;max-width:500px;line-height:1.6}
  .hero .hero-tag{font-size:1.1rem;font-weight:700;color:var(--ink);margin:0 0 32px;max-width:500px;line-height:1.5}
  .hero-cta{display:flex;gap:16px;flex-wrap:wrap}
  .hero-media{position:relative;align-self:center;display:flex;justify-content:center}
  .hero-img{display:block;width:100%;height:auto;max-height:620px;object-fit:contain}

  /* ---------- Section scaffolding ---------- */
  section{padding:90px 0}
  .soft{background:var(--bg-soft)}
  .eyebrow{display:inline-block;font-size:.8rem;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--green-light);background:#e7efe9;padding:6px 14px;border-radius:30px;margin-bottom:16px}
  .sec-head{text-align:center;max-width:680px;margin:0 auto 54px}
  .sec-head h2{font-size:clamp(2rem,3.5vw,2.7rem);color:var(--green);font-weight:800;letter-spacing:-.5px}
  .sec-head p{color:var(--muted);font-size:1.15rem;margin-top:14px}

  /* ---------- Services ---------- */
  .cards{display:grid;grid-template-columns:repeat(3,1fr);gap:26px}
  .card{position:relative;overflow:hidden;background:#fff;border:1px solid var(--line);border-top:2px solid var(--green-dark);border-radius:18px;padding:32px 30px 28px;transition:.2s;box-shadow:0 10px 30px rgba(20,48,38,.05)}
  .card:hover{transform:translateY(-4px);box-shadow:0 16px 38px rgba(20,48,38,.1)}
  .card .cic{position:relative;z-index:2;width:64px;height:64px;border-radius:16px;background:linear-gradient(135deg,#e9f2eb,#d7e8db);display:grid;place-items:center;color:var(--green-dark);margin-bottom:24px}
  .card .wm{position:absolute;top:24px;right:14px;color:var(--green);opacity:.08;z-index:1;pointer-events:none}
  .card h3{position:relative;z-index:2;font-size:1.35rem;color:var(--green);margin-bottom:14px;font-weight:800}
  .card .accent{position:relative;z-index:2;width:46px;height:3px;border-radius:3px;background:var(--green-light);opacity:.55;margin-bottom:18px}
  .card p{position:relative;z-index:2;color:var(--muted);line-height:1.6}
  .card .cdiv{display:flex;align-items:center;justify-content:center;gap:10px;margin-top:28px}
  .card .cdiv .ln{width:42px;height:2px;background:var(--green-light);opacity:.4}
  .card .cdiv .dt{width:9px;height:9px;border-radius:50%;background:var(--green-dark)}

  /* ---------- How it works ---------- */
  #how{background:var(--bg-soft)}
  .how-eyebrow{display:inline-block;font-size:.8rem;font-weight:800;letter-spacing:2px;text-transform:uppercase;color:#fff;background:var(--green-dark);padding:9px 22px;border-radius:30px;margin-bottom:18px}
  .steps-conn{position:relative;height:14px;margin:50px 0 28px}
  .steps-conn .line{position:absolute;top:50%;left:12.5%;right:12.5%;height:2px;background:var(--green-light);opacity:.4;transform:translateY(-50%)}
  .steps-conn .pt{position:absolute;top:50%;width:14px;height:14px;border-radius:50%;background:var(--green-dark);transform:translate(-50%,-50%)}
  .steps-conn .p1{left:12.5%}.steps-conn .p2{left:37.5%}.steps-conn .p3{left:62.5%}.steps-conn .p4{left:87.5%}
  .steps-conn .arr{position:absolute;top:50%;width:0;height:0;border-left:8px solid var(--green-dark);border-top:5px solid transparent;border-bottom:5px solid transparent;transform:translateY(-50%)}
  .steps-conn .a2{left:35%}.steps-conn .a3{left:60%}.steps-conn .a4{left:85%}
  .steps{display:grid;grid-template-columns:repeat(4,1fr);gap:26px}
  .step{position:relative;background:#fff;border:1px solid var(--line);border-radius:18px;padding:48px 24px 30px;text-align:center;box-shadow:0 10px 30px rgba(20,48,38,.06)}
  .step .num{position:absolute;top:-22px;left:50%;transform:translateX(-50%);width:46px;height:46px;border-radius:50%;background:var(--green-dark);color:#fff;font-size:1.2rem;font-weight:800;display:grid;place-items:center;border:4px solid #fff;box-shadow:0 4px 12px rgba(20,48,38,.18)}
  .step-icon{height:90px;display:flex;align-items:center;justify-content:center;margin:4px 0 18px}
  .step-icon svg{width:auto;height:82px}
  .step h4{font-size:1.2rem;margin-bottom:10px;color:var(--green)}
  .step p{color:var(--muted);font-size:.97rem;line-height:1.55}
  .steps-foot{display:flex;align-items:center;justify-content:center;gap:14px;margin-top:48px}
  .steps-foot .ln{width:64px;height:2px;background:var(--green-light);opacity:.4}
  .steps-foot .ck{width:32px;height:32px;border-radius:50%;background:var(--green-dark);display:grid;place-items:center;color:#fff}

  /* ---------- How we diagnose ---------- */
  .diag-cards{display:grid;grid-template-columns:repeat(3,1fr);gap:26px}
  .dcard{position:relative;background:#fff;border:1px solid var(--line);border-top:2px solid var(--green-dark);border-radius:18px;padding:34px 30px;box-shadow:0 10px 30px rgba(20,48,38,.05)}
  .dcard .dstep{position:absolute;top:26px;right:30px;font-size:2.2rem;font-weight:800;color:var(--green-light);opacity:.22;line-height:1}
  .dcard .dic{height:82px;display:flex;align-items:center;justify-content:flex-start;margin-bottom:20px}
  .dcard .dic svg{width:auto;height:78px}
  .dcard h3{font-size:1.25rem;color:var(--green);font-weight:800;margin-bottom:12px}
  .dcard p{color:var(--muted);line-height:1.65}

  /* ---------- About ---------- */
  .about{display:grid;grid-template-columns:1fr 1fr;gap:50px;align-items:center}
  .about-media{min-height:380px;border-radius:var(--radius);background:linear-gradient(120deg,#e9efe9,#cdddd0);display:grid;place-items:center;color:var(--green-light)}
  .about-media svg{width:110px;height:110px;opacity:.55}
  .about h2{font-size:clamp(1.9rem,3vw,2.5rem);color:var(--green);margin-bottom:18px;font-weight:800}
  .about p{color:var(--muted);margin-bottom:16px;font-size:1.08rem}

  /* ---------- Reviews carousel ---------- */
  .rev-slider{display:flex;align-items:center;justify-content:center;gap:18px;max-width:760px;margin:0 auto}
  .rev-arrow{flex:none;width:46px;height:46px;border-radius:50%;border:1px solid var(--line);background:#fff;color:var(--green);display:grid;place-items:center;cursor:pointer;transition:.15s}
  .rev-arrow:hover{background:var(--green);color:#fff;border-color:var(--green)}
  .rev-card{position:relative;background:#fff;border:1px solid var(--line);border-radius:var(--radius);padding:34px 36px;flex:1;min-height:236px;box-shadow:0 10px 30px rgba(20,48,38,.06)}
  .rev-g{position:absolute;top:24px;right:28px}
  .rev-stars{color:#fbbc05;font-size:1.25rem;letter-spacing:2px;margin-bottom:14px}
  .rev-text{color:var(--ink);font-size:1.12rem;line-height:1.6;margin-bottom:22px;font-style:italic}
  .rev-author{display:flex;align-items:center;gap:13px}
  .rev-avatar{width:46px;height:46px;border-radius:50%;background:var(--green);color:#fff;display:grid;place-items:center;font-weight:700;font-size:1.05rem;flex:none}
  .rev-author strong{display:block;font-size:1rem}
  .rev-author small{color:var(--muted);font-size:.85rem}
  .rev-dots{display:flex;justify-content:center;gap:9px;margin:24px 0 28px}
  .rev-dots button{width:9px;height:9px;border-radius:50%;border:0;background:#c7d2cb;cursor:pointer;padding:0;transition:.2s}
  .rev-dots button.on{background:var(--green);width:26px;border-radius:5px}
  @media(max-width:640px){
    .rev-arrow{display:none}
    .rev-card{padding:28px 24px}
  }
  .stats{display:flex;gap:36px;margin-top:26px}
  .stat strong{display:block;font-size:2rem;color:var(--green);font-weight:800}
  .stat span{color:var(--muted);font-size:.95rem}

  /* ---------- Service areas ---------- */
  .areas{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:14px;max-width:900px;margin:0 auto}
  .area{background:#fff;border:1px solid var(--line);border-radius:10px;padding:18px 22px;display:flex;align-items:center;gap:12px;font-weight:500}
  .area svg{width:20px;height:20px;color:var(--green);flex:none}
  .areas-note{text-align:center;color:var(--muted);margin:26px auto 0;max-width:900px}
  .areas-note a{color:var(--green);font-weight:600;text-decoration:underline}

  /* ---------- Contact ---------- */
  .contact{display:grid;grid-template-columns:1fr 1fr;gap:50px;align-items:start}
  .contact-info h2{font-size:clamp(1.9rem,3vw,2.5rem);color:var(--green);margin-bottom:16px;font-weight:800}
  .contact-info p{color:var(--muted);font-size:1.08rem;margin-bottom:26px}
  .contact-eyebrow{display:inline-flex;align-items:center;gap:10px;font-size:.8rem;font-weight:800;letter-spacing:2px;text-transform:uppercase;color:var(--green);background:#e7efe9;padding:8px 18px 8px 8px;border-radius:30px;margin-bottom:18px}
  .contact-eyebrow .ei{width:30px;height:30px;border-radius:50%;background:var(--green);color:#fff;display:grid;place-items:center;flex:none}
  .contact-eyebrow .ei svg{width:15px;height:15px}
  .contact-illus{margin:0 0 30px;border-radius:var(--radius);overflow:hidden;background:#eef3ef;border:1px solid var(--line)}
  .contact-illus img{display:block;width:100%;height:auto}
  .cinfo-grid{display:flex;flex-wrap:wrap;gap:30px 34px;margin-top:30px}
  .cinfo-row{display:flex;align-items:flex-start;gap:14px}
  .cinfo-row .cic{width:46px;height:46px;border-radius:12px;background:#eef3ee;display:grid;place-items:center;color:var(--green);flex:none}
  .cinfo-row small{display:block;color:var(--muted);font-size:.85rem;margin-bottom:3px}
  .cinfo-row strong{font-size:1rem;display:block;line-height:1.35}
  form{background:#fff;border:1px solid var(--line);border-radius:var(--radius);padding:34px}
  .field{margin-bottom:18px}
  .field label{display:block;font-weight:600;margin-bottom:7px;font-size:.95rem}
  .field input,.field select,.field textarea{width:100%;padding:13px 15px;border:1px solid var(--line);border-radius:9px;font-family:inherit;font-size:1rem;background:#fcfdfc}
  .field input:focus,.field select:focus,.field textarea:focus{outline:none;border-color:var(--green)}
  .row2{display:grid;grid-template-columns:1fr 1fr;gap:18px}
  form .btn-primary{width:100%;justify-content:center;margin-top:6px}

  /* ---------- Footer ---------- */
  footer{background:#162d26;color:#cfe0d6;padding:64px 0 26px}
  .fgrid{display:grid;grid-template-columns:1.6fr 1fr;gap:34px;margin-bottom:40px}
  footer .logo{color:#fff}
  footer .flogo img{width:84px;height:84px;border-radius:16px;display:block}
  footer h5{color:#fff;font-size:1rem;margin-bottom:16px;letter-spacing:.5px}
  footer ul{list-style:none}
  footer ul li{margin-bottom:10px}
  footer ul a:hover{color:#fff}
  footer .fdesc{margin:16px 0;max-width:280px;font-size:.95rem}
  .fbar{border-top:1px solid rgba(255,255,255,.12);padding-top:22px;display:flex;justify-content:space-between;flex-wrap:wrap;gap:10px;font-size:.85rem;color:#9fb6a9}

  /* ---------- Booking modal ---------- */
  .modal-overlay{position:fixed;inset:0;z-index:200;background:rgba(15,30,24,.55);backdrop-filter:blur(3px);
    display:none;align-items:flex-start;justify-content:center;padding:40px 16px;overflow-y:auto}
  .modal-overlay.open{display:flex}
  .modal{background:#fff;width:100%;max-width:540px;border-radius:18px;box-shadow:0 30px 80px rgba(0,0,0,.3);
    overflow:hidden;animation:pop .25s ease}
  @keyframes pop{from{transform:translateY(14px);opacity:0}to{transform:none;opacity:1}}
  .modal-head{display:flex;align-items:center;justify-content:space-between;padding:24px 28px 0}
  .modal-head h3{font-size:1.5rem;color:var(--ink);font-weight:800}
  .modal-close{background:none;border:0;cursor:pointer;color:var(--muted);padding:6px;line-height:0;border-radius:8px}
  .modal-close:hover{background:var(--bg-soft);color:var(--ink)}
  .modal-body{padding:20px 28px 4px;max-height:none}

  /* stepper */
  .stepper{display:flex;align-items:center;justify-content:space-between;margin:18px 0 26px}
  .stepper .st{display:flex;flex-direction:column;align-items:center;gap:6px;flex:none;position:relative;z-index:2}
  .stepper .st .dot{width:30px;height:30px;border-radius:50%;background:#fff;border:2px solid var(--line);
    color:var(--muted);font-weight:700;font-size:.85rem;display:grid;place-items:center;transition:.2s}
  .stepper .st small{font-size:.78rem;color:var(--muted);font-weight:500}
  .stepper .st.active .dot{background:var(--green);border-color:var(--green);color:#fff}
  .stepper .st.active small{color:var(--green);font-weight:700}
  .stepper .st.done .dot{background:var(--green-light);border-color:var(--green-light);color:#fff}
  .stepper .bar{flex:1;height:2px;background:var(--line);margin:0 6px;position:relative;top:-9px;z-index:1}

  /* block */
  .bk-block{margin-bottom:26px}
  .bk-block .bk-h{display:flex;align-items:center;gap:10px;margin-bottom:4px}
  .bk-block .bk-n{width:24px;height:24px;border-radius:50%;background:var(--green);color:#fff;font-size:.8rem;font-weight:700;display:grid;place-items:center;flex:none}
  .bk-block .bk-h h4{font-size:1.05rem;color:var(--ink)}
  .bk-block .bk-sub{color:var(--muted);font-size:.9rem;margin:0 0 14px 34px}

  /* service options */
  .svc{display:flex;align-items:center;gap:14px;border:1px solid var(--line);border-radius:12px;padding:14px 16px;margin-bottom:10px;cursor:pointer;transition:.15s}
  .svc:hover{border-color:#c4d6ca;background:#fafcfb}
  .svc.sel{border-color:var(--green);box-shadow:0 0 0 1px var(--green) inset;background:#f5faf7}
  .svc .radio{width:20px;height:20px;border-radius:50%;border:2px solid #c4ccc7;flex:none;display:grid;place-items:center;transition:.15s}
  .svc.sel .radio{border-color:var(--green)}
  .svc.sel .radio::after{content:"";width:10px;height:10px;border-radius:50%;background:var(--green)}
  .svc .check{width:20px;height:20px;border-radius:6px;border:2px solid #c4ccc7;flex:none;display:grid;place-items:center;transition:.15s}
  .svc.sel .check{border-color:var(--green);background:var(--green)}
  .svc.sel .check::after{content:"";width:6px;height:10px;border:solid #fff;border-width:0 2px 2px 0;transform:rotate(45deg);margin-top:-2px}
  .svc .svc-ic{width:38px;height:38px;border-radius:9px;background:var(--bg-soft);display:grid;place-items:center;color:var(--green);flex:none}
  .svc .svc-t{flex:1}
  .svc .svc-t strong{display:block;font-size:1rem;color:var(--ink)}
  .svc .svc-t span{font-size:.85rem;color:var(--muted)}

  /* calendar */
  .cal{border:1px solid var(--line);border-radius:12px;padding:16px}
  .cal-top{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}
  .cal-top strong{font-size:1rem}
  .cal-nav{background:none;border:1px solid var(--line);border-radius:8px;width:32px;height:32px;cursor:pointer;color:var(--ink);display:grid;place-items:center}
  .cal-nav:hover{background:var(--bg-soft)}
  .cal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:2px;text-align:center}
  .cal-grid .dow{font-size:.75rem;color:var(--muted);font-weight:600;padding:6px 0}
  .cal-day{aspect-ratio:1;border:0;background:none;border-radius:50%;cursor:pointer;font-size:.9rem;color:var(--ink);display:grid;place-items:center}
  .cal-day:hover:not(:disabled){background:var(--bg-soft)}
  .cal-day.muted{color:#c4ccc7;cursor:default}
  .cal-day:disabled{color:#d6dcd8;cursor:not-allowed}
  .cal-day.sel{background:var(--green);color:#fff;font-weight:700}

  /* time slots */
  .slots{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}
  .slot{border:1px solid var(--line);border-radius:10px;padding:12px 6px;text-align:center;cursor:pointer;background:#fff;transition:.15s;font-size:.85rem}
  .slot:hover{border-color:#c4d6ca}
  .slot small{display:block;color:var(--muted);font-size:.72rem;margin-bottom:3px}
  .slot strong{font-weight:600;color:var(--ink);font-size:.85rem}
  .slot.sel{border-color:var(--green);background:#f5faf7;box-shadow:0 0 0 1px var(--green) inset}
  .slot.more{grid-column:span 4;color:var(--green);font-weight:600;border-style:dashed}

  /* identifier toggle */
  .toggle{display:flex;gap:8px;background:var(--bg-soft);border:1px solid var(--line);border-radius:10px;padding:4px}
  .toggle .tg{flex:1;border:0;background:transparent;padding:11px;border-radius:8px;cursor:pointer;font-weight:600;color:var(--muted);font-family:inherit;font-size:.95rem;transition:.15s}
  .toggle .tg:hover{color:var(--ink)}
  .toggle .tg.sel{background:#fff;color:var(--green);box-shadow:0 1px 3px rgba(0,0,0,.1)}
  .svc-toggle{flex-wrap:wrap}
  .svc-toggle .tg{flex:1 1 auto;white-space:nowrap}

  /* details */
  .bk-block .row2{margin:0}
  .bk-block .field{margin-bottom:14px}
  .bk-block .field label{font-size:.85rem}

  /* summary */
  .summary{border:1px solid var(--line);border-radius:12px;padding:16px;display:flex;gap:14px;align-items:flex-start;background:#fafcfb}
  .summary .sic{width:42px;height:42px;border-radius:10px;background:var(--green);color:#fff;display:grid;place-items:center;flex:none}
  .summary .s-t small{color:var(--muted);font-size:.8rem}
  .summary .s-t strong{display:block;font-size:1rem;color:var(--ink);margin:2px 0}
  .summary .s-t span{font-size:.88rem;color:var(--muted);display:block}
  .est{margin-top:14px;background:#eef5f0;border:1px solid #d8e7dd;border-radius:12px;padding:16px 18px;display:flex;align-items:center;justify-content:space-between}
  .est .est-l{display:flex;align-items:center;gap:10px}
  .est .est-l svg{color:var(--green)}
  .est .est-amt{font-size:1.5rem;font-weight:800;color:var(--green)}
  .est .est-note{font-size:.72rem;color:var(--muted)}
  .est small{display:block;font-size:.72rem;color:var(--muted);font-weight:500}

  /* perks */
  .perks{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:16px}
  .perk{display:flex;align-items:center;gap:10px;border:1px solid var(--line);border-radius:10px;padding:12px;font-size:.82rem;color:var(--muted)}
  .perk svg{color:var(--green);flex:none}

  /* footer of modal */
  .modal-foot{position:sticky;bottom:0;background:#fff;border-top:1px solid var(--line);padding:16px 28px;display:flex;gap:12px}
  .modal-foot .btn{flex:1;justify-content:center}
  .modal-note{text-align:center;font-size:.75rem;color:var(--muted);padding:0 28px 18px}

  /* ---------- Responsive ---------- */
  @media(max-width:960px){
    .navlinks,.phone,.divider{display:none}
    .menu-btn{display:flex}
    .hero .wrap{grid-template-columns:1fr}
    .hero-media{display:none}
    .hero-copy{padding:48px 0}
    .cards,.steps,.diag-cards{grid-template-columns:1fr 1fr}
    .steps-conn{display:none}
    .about,.contact{grid-template-columns:1fr}
    .fgrid{grid-template-columns:1fr 1fr}
    .mobile-open .navlinks{display:flex;position:absolute;top:92px;left:0;right:0;flex-direction:column;background:#fff;border-bottom:1px solid var(--line);padding:18px 28px;gap:14px}
  }
  @media(max-width:560px){
    .cards,.steps,.fgrid,.row2,.diag-cards{grid-template-columns:1fr}
    .hero-cta{flex-direction:column}
    .hero-cta .btn{width:100%;justify-content:center}
    .slots{grid-template-columns:repeat(2,1fr)}
    .slot.more{grid-column:span 2}
    .perks{grid-template-columns:1fr}
    .stepper .st small{font-size:.68rem}
    .modal-body,.modal-head,.modal-foot,.modal-note{padding-left:18px;padding-right:18px}
  }
</style>